# 复制为 .env 后修改（.env 不要提交到代码仓库）。docker compose 会自动读取同目录下的 .env。
POSTGRES_DB=educa
POSTGRES_USER=educa
POSTGRES_PASSWORD=change-me
SITE_DOMAIN=educaproject.com
# 不设置 EMAIL_HOST 时，邮件只打印到日志
EMAIL_HOST=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
